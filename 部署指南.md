# 部署指南 - Vercel + Render

本指南将教你如何把「Peter 的小世界」网站部署到互联网上，让所有人都能通过域名访问。

## 📋 准备工作

在开始之前，你需要注册以下账号（都是免费的）：

1. **GitHub**：存放代码
   - 注册地址：https://github.com/signup
   - 用邮箱注册就行

2. **Vercel**：部署前端网站
   - 注册地址：https://vercel.com/signup
   - 直接用 GitHub 账号登录

3. **Render**：部署后端服务
   - 注册地址：https://dashboard.render.com/register
   - 直接用 GitHub 账号登录

---

## 🚀 第一步：把代码上传到 GitHub

### 方法一：用 Git 命令行（推荐）

如果你电脑上安装了 Git：

1. 打开命令行，进入项目目录：
   ```bash
   cd E:\桌面\peter-world
   ```

2. 初始化 Git 仓库：
   ```bash
   git init
   ```

3. 添加所有文件：
   ```bash
   git add .
   ```

4. 提交代码：
   ```bash
   git commit -m "初始提交"
   ```

5. 在 GitHub 上创建一个新仓库（名字比如叫 `peter-world`），然后推送代码：
   ```bash
   git branch -M main
   git remote add origin https://github.com/你的用户名/peter-world.git
   git push -u origin main
   ```

### 方法二：用 GitHub Desktop（图形界面，更简单）

1. 下载 GitHub Desktop：https://desktop.github.com/
2. 安装并登录你的 GitHub 账号
3. 点击 "Add" → "Add existing repository"
4. 选择项目文件夹 `E:\桌面\peter-world`
5. 点击 "Create repository"
6. 点击 "Publish repository" 推送到 GitHub

---

## 🔧 第二步：部署后端到 Render

### 操作步骤

1. 打开 Render 并登录：https://dashboard.render.com/

2. 点击右上角的 **"New +"** → 选择 **"Web Service"**

3. 连接你的 GitHub 仓库
   - 点击 "Connect account" 连接 GitHub
   - 选择刚才创建的 `peter-world` 仓库
   - 点击 "Connect"

4. 配置服务
   - **Name**：`peter-world-backend`（随便起，好记就行）
   - **Region**：选一个离你近的，比如 `Singapore`（新加坡）
   - **Branch**：`main`
   - **Root Directory**：`backend` ⚠️ 重要！一定要填这个
   - **Runtime**：`Node`
   - **Build Command**：`npm install && npm run build`
   - **Start Command**：`npm start`
   - **Instance Type**：选 **Free**（免费）

5. 点击 **"Advanced"**，添加环境变量：
   - 点击 "Add Environment Variable"
   - 添加以下变量：
     | Key | Value | 说明 |
     |-----|-------|------|
     | `NODE_ENV` | `production` | 生产环境 |
     | `JWT_SECRET` | 随便写一串复杂的字符 | 用于加密登录令牌 |
     | `ADMIN_USERNAME` | `admin` | 管理员用户名 |
     | `ADMIN_PASSWORD` | 你的密码 | 管理员密码（一定要改！） |

6. 点击 **"Create Web Service"** 开始部署

7. 等待部署完成（大约 2-5 分钟）
   - 部署成功后，你会得到一个地址，比如：`https://peter-world-backend.onrender.com`
   - **把这个地址记下来，后面要用！**

### 验证后端是否部署成功

在浏览器访问：
```
https://你的后端地址/api/health
```
如果看到 `{"status":"ok",...}` 就说明成功了！

---

## 🎨 第三步：部署前端到 Vercel

### 操作步骤

1. 打开 Vercel 并登录：https://vercel.com/

2. 点击 **"Add New..."** → 选择 **"Project"**

3. 导入你的 GitHub 仓库
   - 找到 `peter-world` 仓库
   - 点击 "Import"

4. 配置项目
   - **Project Name**：`peter-world`（随便起）
   - **Framework Preset**：Vite（应该会自动检测到）
   - **Root Directory**：点击 "Edit"，选择 `frontend` ⚠️ 重要！
   - 其他保持默认

5. 配置环境变量
   - 找到 "Environment Variables" 部分
   - 添加一个变量：
     | Variable Name | Value |
     |---------------|-------|
     | `VITE_API_URL` | `https://你的后端地址` |
   
   ⚠️ 注意：把"你的后端地址"换成上一步 Render 给你的地址，比如 `https://peter-world-backend.onrender.com`

6. 点击 **"Deploy"** 开始部署

7. 等待部署完成（大约 1-2 分钟）
   - 部署成功后，你会得到一个地址，比如：`https://peter-world.vercel.app`
   - 现在你已经可以通过这个地址访问网站了！

---

## 🌐 第四步：绑定你的域名 peter16.top

### 1. 在 Vercel 添加域名

1. 打开 Vercel 项目页面
2. 点击 **"Settings"** → **"Domains"**
3. 输入你的域名：`peter16.top`
4. 点击 "Add"
5. Vercel 会告诉你需要配置什么 DNS 记录

### 2. 在阿里云配置 DNS 解析

1. 打开阿里云域名控制台：https://dc.console.aliyun.com/
2. 找到你的域名 `peter16.top`，点击 "解析"
3. 添加以下记录：

   **记录 1 - 主域名：**
   - 记录类型：`CNAME`
   - 主机记录：`@`
   - 记录值：Vercel 给你的那个地址（比如 `peter-world.vercel.app`）
   - TTL：`10分钟`

   **记录 2 - www 子域名：**
   - 记录类型：`CNAME`
   - 主机记录：`www`
   - 记录值：Vercel 给你的那个地址
   - TTL：`10分钟`

4. 保存，等待 DNS 生效（通常几分钟到几小时）

### 3. 验证域名

回到 Vercel 的 Domains 页面，等域名状态变成 "Valid" 就成功了！

Vercel 会自动给你配置 HTTPS 证书，非常方便。

---

## ✅ 完成！

现在你可以通过以下地址访问你的网站：

- 主域名：https://peter16.top
- www 域名：https://www.peter16.top
- Vercel 备用地址：https://xxx.vercel.app
- 后台地址：https://peter16.top/admin

---

## ⚠️ 重要说明

### 关于免费套餐的限制

**Render 免费套餐：**
- 🟢 完全免费
- 🟡 服务 15 分钟没人访问会休眠，第一次访问需要等几秒唤醒
- 🔴 **文件系统是临时的**：重新部署后，上传的照片、音乐、数据库会丢失！
  - 这是免费套餐的限制
  - 如果数据很重要，可以升级 Render 付费套餐（每月约 7 美元），或者使用外部存储

**Vercel 免费套餐：**
- 🟢 完全免费
- 🟢 速度快，全球 CDN
- 🟢 自动 HTTPS
- 🟡 每月有 100GB 流量限制（个人网站完全够用）

### 数据备份建议

因为 Render 免费套餐的文件是临时的，建议你：
1. 定期在后台导出/备份重要数据
2. 照片和音乐文件在本地保留备份
3. 如果网站数据很重要，可以考虑升级 Render 付费套餐

---

## ❓ 常见问题

**Q: 部署后网站打不开？**
A: 等几分钟，第一次部署需要时间。如果还是不行，检查 Render 和 Vercel 的日志。

**Q: 后台登录不进去？**
A: 检查前端的 `VITE_API_URL` 环境变量是否正确配置了后端地址。

**Q: 上传的照片不见了？**
A: Render 免费套餐重新部署后文件会丢失，这是正常的。可以考虑升级套餐或使用对象存储。

**Q: 国内访问速度慢？**
A: Vercel 和 Render 在国内访问速度一般。如果追求速度，可以考虑用国内的云服务（比如阿里云、腾讯云）。

---

## 🎉 恭喜！

你已经拥有了一个完全属于自己的个人网站！
